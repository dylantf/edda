module Edda

# Core public API for the Edda web framework.
#
# Implementation lives in focused modules, while this barrel keeps the common
# `import Edda (...)` path stable for applications.

import Edda.Request (pub ..)
import Edda.Response (pub ..)
import Edda.Router (pub ..)
import Edda.Static (pub ..)
import Edda.Middleware (pub ..)
import Edda.App (pub ..)
